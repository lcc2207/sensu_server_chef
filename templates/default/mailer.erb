{
    "mailer": {
        "mail_from": "<%= @mail_from %>",
        "mail_to": "<%= @mail_to %>",
        "smtp_address": "<%= @smtp_address%>",
        "smtp_port": "<%= @smtp_port %>",
        "smtp_domain": "<%= @smtp_domain %>"
    }
}
